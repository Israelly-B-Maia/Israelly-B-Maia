import 'package:flutter/material.dart';

class TelaRecarregue extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recarregue'),
      ),
      body: Center(
        child: Text('Esta é a tela recarregue.'),
      ),
    );
  }
}